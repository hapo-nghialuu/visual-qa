import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#006242",
        secondary: "#114734",
        accent: "#A7C5EE",
        "accent-warm": "#F2EA9D",
        foreground: "#101820",
      },
    },
  },
};

export default config;
